# Homelab v2.4.4 Plus - Rollback and polish updates

Added after real install testing:

- Maintenance rollback script: `maintenance/reset/rollback-everything-except-truenas.sh`
  - Destroys VM102-107 only
  - Preserves VM101 TrueNAS, `nvme-vm`, secrets, and Proxmox host state
  - Requires typing `ROLLBACK`
- Non-TrueNAS Ubuntu VMs now use Proxmox display `none` for headless SSH workflow.
- SMTP test helper supports `all` profile and clearer 535 authentication diagnostics.
- Bazarr config performs best-effort announcement/notification cleanup.
- Additionals Auth/SSO experiment menu added for Jellyfin/Seerr/Cloudflare Access Google login notes.

Recommended test loop:

1. Upload this repo version to GitHub.
2. Run maintenance rollback from an existing Proxmox host.
3. Continue from install menu step 4.
